<div class="container">

    <% if(sessionFlash){ %>
        <div class="<%= sessionFlash.type %>">
            <%= sessionFlash.message %>
        </div>
    <% } %>

    <h2>Address Book Services</h2>
    <hr><br>
    <a type="button" class="btn btn-primary" href="/add_address">Add New Address</a>
    <br><hr>
</div>
